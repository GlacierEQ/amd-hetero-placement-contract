[project]
name = "amd-hetero-placement-contract"
version = "1.0.0"
description = "Deterministic heterogeneous workload placement engine"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
