[project]
name = "amd-hetero-placement-contract"
version = "0.0.1"
description = "Scaffold: Heterogeneous Placement Contract (independent AMD lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
